<h1>タスク詳細(id:<%= @task.id %>)</h1>
<table>
  <tr>
    <th>タスク内容</th>
    <td><%= @task.content %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @task.status %></td>
  </tr>
</table>
<%= link_to "タスク一覧に戻る", tasks_path %>
<%= link_to "このタスクを編集する", edit_task_path(@task) %>
<%= link_to "このタスクを削除する", @message, method: :delete, data: {confirm: "本当に削除してもよろしいですか？"} %>
